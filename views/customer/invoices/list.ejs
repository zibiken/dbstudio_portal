<section class="customer-invoices">
  <header class="customer-section-header">
    <h1>Invoices</h1>
  </header>

  <% if (rows.length === 0) { %>
    <p class="muted">You don't have any invoices yet.</p>
  <% } else { %>
    <table class="data-table">
      <thead>
        <tr>
          <th>Number</th>
          <th>Amount</th>
          <th>Issued</th>
          <th>Due</th>
          <th>Status</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <% for (const r of rows) { %>
          <tr>
            <td><a href="/customer/invoices/<%= r.id %>"><%= r.invoice_number %></a></td>
            <td><%= (Number(r.amount_cents)/100).toFixed(2) %> <%= r.currency %></td>
            <td><%= euDate(r.issued_on) %></td>
            <td><%= euDate(r.due_on) %><% if (r.overdue) { %> <span class="badge badge-warning">overdue</span><% } %></td>
            <td><%= r.status %></td>
            <td><a href="/customer/invoices/<%= r.id %>">Open →</a></td>
          </tr>
        <% } %>
      </tbody>
    </table>
  <% } %>
</section>
