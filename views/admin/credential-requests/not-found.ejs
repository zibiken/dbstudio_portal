<%- include('../../components/_page-header', {
  eyebrow: 'ADMIN',
  title: 'Credential request not found'
}) %>

<%- include('../../components/_alert', { variant: 'warn', body: 'That credential request does not exist or does not belong to this customer.' }) %>

<div class="form-actions">
  <%- include('../../components/_button', { variant: 'secondary', size: 'md', href: '/admin/customers', label: 'Back to customers' }) %>
</div>
